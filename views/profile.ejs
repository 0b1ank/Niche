<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Niche Cafes</title>
    <link rel="stylesheet" href="login.css">
    <link rel="stylesheet" href="style.css">
    <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
</head>
<body>
    <aside class="sidebar">
        <div class="sidebar-header">
        </div>
        <ul class="nav-links">
            <li><a href="index.html" class="active"><i class='bx bx-home-alt-2'></i> <span></span></a></li>
            <li><a href="#"><i class="bx bx-book-add"></i> <span></span></a></li>
            <!-- <li><a href="#"><i class='bx bx-chat'></i> <span>Profile</span></a></li> -->
            <li><a href="#"><i class='bx bx-list-ul'></i><span></span></a></li>
        </ul>
        
    </aside>

    <main class="main-content">
        <!-- <div class="login-header"> -->
            <h2>Profile</h2>
            <div class="profile-wrapper">
                <div class="avatar-profile" id="avatarPreview">
                    <i class='bx bx-user'></i>
                </div>

                <div class="profile-group">
                    <h3 for="username"><strong>Username: </strong></h3>
                    <div>
                        <p> mockUsername</p>
                    </div>
                </div>

                <div class="profile-group">
                    <h3 for="username">Email: </h3>
                    <div>
                        <p> mockEmail</p>
                    </div>
                </div>
            </div>
            

        <!-- New Account Processing Form Matrix -->
        <form id="newAccountForm">
            <div class="input-group">
                <label for="username">Username:</label>
                <div class="input-wrapper">
                    <i class='bx bx-user'></i>
                    <!-- <input type="text" id="username" placeholder="Enter your username" required> -->
                </div>
            </div>

            <div class="input-group">
                <label for="username">Email:</label>
                <div class="input-wrapper">
                    <i class='bx bx-user'></i>
                    <!-- <input type="text" id="email" placeholder="Enter your email" required> -->
                </div>
            </div>

            <div class="input-group">
                <label for="password">Password:</label>
                <div class="input-wrapper">
                    <i class='bx bx-lock-alt'></i>
                    <!-- <input type="password" id="password" placeholder="Enter your password" required> -->
                </div>
            </div>
            <br/>

            <!-- Main Action Control Container -->
            <div class="action-group">
                
                <!-- Primary Stacked Button (Top) -->
                <input type="file" id="realFileInput" accept="image/*" style="display: none;">
    
                <!-- Container holding the button and its new preview friend side-by-side
                <div class="upload-row">
                    
                    
                </div> -->
                
                <!-- Secondary Role Selection Row (Bottom) -->
                <label for="password">Account Type</label>
                <div class="role-row">
                    <a href="#" class="user-btn role-btn role-btn-owner">
                        <i class='bx bx-briefcase'></i>
                        <span>Owner</span>
                    </a>
                    <a href="#" class="user-btn role-btn role-btn-user active">
                        <i class='bx bx-user'></i>
                        <span>User</span>
                    </a>
                    
                </div>
                <a href="index.html" class="submit-btn">Log Out</a>
            </div>

        </form>

    </main>

    <script>
        // Temporary intercept logic managing UI testing flow
        document.getElementById('newAccountForm').addEventListener('newAccount', (e) => {
            e.preventDefault(); // Stop page from refreshing on form submit
            
            const user = document.getElementById('username').value;
            console.log("Authenticating user session:", user);
            
            // For now, redirect straight back to your dashboard page on submission
            window.location.href = "index.html"; 
        });

        document.addEventListener('DOMContentLoaded', () => {
            const uploadTriggerBtn = document.getElementById('uploadTriggerBtn');
            const realFileInput = document.getElementById('realFileInput');
            const avatarPreview = document.getElementById('avatarPreview');

            // 1. Forward the click event from your fancy button over to the hidden input tag
            uploadTriggerBtn.addEventListener('click', () => {
                realFileInput.click();
            });

            // 2. Listen for when a file is successfully chosen by the browser user
            realFileInput.addEventListener('change', function() {
                const file = this.files[0]; // Grab the first chosen file

                if (file) {
                    const reader = new FileReader();

                    // When the file reader engine successfully processes the document stream:
                    reader.onload = function(e) {
                        // Generate a real dynamic image tag containing the processed data URL
                        avatarPreview.innerHTML = `<img src="${e.target.result}" alt="User Profile Preview">`;
                    }

                    // Instruct the browser to read the local target file into a base64 encoded URL string
                    reader.readAsDataURL(file);
                }
            });
        });
        // 1. Initialize a state tracking variable. Default to 'user'.
        let chosenRole = 'user'; 

        // 2. Grab references to both elements in the DOM
        const userBtn = document.querySelector('.role-btn-user');
        const ownerBtn = document.querySelector('.role-btn-owner');

        // 3. Setup the Selection Listener Logic for the USER button
        userBtn.addEventListener('click', (event) => {
            event.preventDefault(); // Prevents the link '#' from jumping the page scroll
            
            chosenRole = 'user';    // Update our tracking variable
            
            userBtn.classList.add('active');     // Light up the user button
            ownerBtn.classList.remove('active'); // Turn off the owner button
            console.log("Current role selection updated to:", chosenRole);
        });

        // 4. Setup the Selection Listener Logic for the OWNER button
        ownerBtn.addEventListener('click', (event) => {
            event.preventDefault();
            
            chosenRole = 'owner';   // Update our tracking variable
            
            ownerBtn.classList.add('active');    // Light up the owner button
            userBtn.classList.remove('active');  // Turn off the user button
            console.log("Current role selection updated to:", chosenRole);
        });

        // 5. Example Form Submit function showing how that variable is used
        function registerAccount() {
            const registrationData = {
                username: document.getElementById('username').value, // assuming you have an input with this ID
                roleId: chosenRole                                   // This safely reads 'user' or 'owner'!
            };

            console.log("Submitting account payload to database:", registrationData);
            // Next step: send registrationData to your database server!
        }

    </script>

</body>
</html>