<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Niche Cafes - Register</title>
    <link rel="stylesheet" href="login.css">
    <link rel="stylesheet" href="style.css">
    <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
</head>
<body>
    <a href="/login" class="fixed-login-btn">
        <i class='bx bx-user-circle'></i>
        <span>Log In</span>
    </a>

    <aside class="sidebar">
        <div class="sidebar-header"></div>
        <ul class="nav-links">
            <li><a href="/" class="active"><i class='bx bx-home-alt-2'></i> <span>Home</span></a></li>
            <li><a href="#"><i class="bx bx-book-add"></i> <span>Add</span></a></li>
        </ul>
    </aside>

    <main class="main-content">
        <div class="login-header">
            <h2>Create a New Account</h2>
            <p>Please enter your information</p>
        </div>

        <%# Express POST form with multipart/form-data for image uploads %>
        <form action="/register" method="POST" enctype="multipart/form-data" id="newAccountForm">
            <div class="input-group">
                <label for="username">Username</label>
                <div class="input-wrapper">
                    <i class='bx bx-user'></i>
                    <input type="text" id="username" name="username" placeholder="Enter your username" required>
                </div>
            </div>

            <div class="input-group">
                <label for="email">Email</label>
                <div class="input-wrapper">
                    <i class='bx bx-envelope'></i>
                    <input type="email" id="email" name="email" placeholder="Enter your email" required>
                </div>
            </div>

            <div class="input-group">
                <label for="password">Password</label>
                <div class="input-wrapper">
                    <i class='bx bx-lock-alt'></i>
                    <input type="password" id="password" name="password" placeholder="Enter your password" required>
                </div>
            </div>
            <br/>

            <!-- Main Action Control Container -->
            <div class="action-group">
                <!-- Profile Image Upload Field (Multer field name: pfp) -->
                <input type="file" id="pfp" name="pfp" accept="image/*" style="display: none;">

                <%# Hidden form input ensuring the chosen role submits to backend %>
                <input type="hidden" name="role" id="roleInput" value="user">

                <div class="role-row">
                    <div class="upload-row">
                        <button type="button" class="user-btn upload-btn" id="uploadTriggerBtn">
                            <i class="bx bx-upload"></i> 
                            <span>Upload Photo</span>
                        </button>

                        <div class="avatar-preview-circle" id="avatarPreview">
                            <i class='bx bx-user'></i>
                        </div>
                    </div>

                    <!-- Account Type Role Selection -->
                    <label>Account Type</label>
                    <button type="button" class="user-btn role-btn role-btn-owner" id="ownerBtn">
                        <i class='bx bx-briefcase'></i>
                        <span>Owner</span>
                    </button>
                    <button type="button" class="user-btn role-btn role-btn-user" id="userBtn">
                        <i class='bx bx-user'></i>
                        <span>User</span>
                    </button>
                </div>
                
                <!-- <button type="submit" class="submit-btn"><a href="/loggedIn" >Register</a></button> -->

                <a href="/loggedIn" class="submit-btn">Register</a>
                <a href="/auth/google" class="create-account-btn">Continue with Google</a>
                <a href="/login" class="create-account-btn">Already have an account? Log in</a>
            </div>
        </form>
        
    </main>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // 1. File Upload Interaction & Live Preview Logic
            const uploadTriggerBtn = document.getElementById('uploadTriggerBtn');
            const realFileInput = document.getElementById('pfp');
            const avatarPreview = document.getElementById('avatarPreview');

            uploadTriggerBtn.addEventListener('click', () => {
                realFileInput.click();
            });

            realFileInput.addEventListener('change', function() {
                const file = this.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = function(e) {
                        avatarPreview.innerHTML = `<img src="${e.target.result}" alt="User Profile Preview">`;
                    }
                    reader.readAsDataURL(file);
                }
            });

            // 2. Role Selector Switcher linked to backend hidden input
            const userBtn = document.getElementById('userBtn');
            const ownerBtn = document.getElementById('ownerBtn');
            const roleInput = document.getElementById('roleInput');

            userBtn.addEventListener('click', (e) => {
                e.preventDefault();
                roleInput.value = 'user';
                userBtn.classList.add('active');
                ownerBtn.classList.remove('active');
            });

            ownerBtn.addEventListener('click', (e) => {
                e.preventDefault();
                roleInput.value = 'owner';
                ownerBtn.classList.add('active');
                userBtn.classList.remove('active');
            });
        });
    </script>
</body>
</html>
